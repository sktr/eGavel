# Server signing key (64-char hex) — the server's key in the 2-of-3 lock.
# Generate with: openssl rand -hex 32
SERVER_PRIVATE_KEY=

# HTTP listen port (web app uses 3000, so server uses 3001 to avoid conflict)
PORT=3001

# SQLite database file path (relative to apps/server)
DB_PATH=data/auction.db

# Seller-paid fee in basis points (0 = free marketplace; 500 = 5%), split at claim time
AUCTION_FEE_BPS=0
